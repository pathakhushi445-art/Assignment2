using StudentLibrary;

class Program
{
    static void Main()
    {
        Student s = new Student();

        Console.WriteLine(s.Name);       // Accessible

        s.ShowPublic();                  // Accessible 
    }
}
